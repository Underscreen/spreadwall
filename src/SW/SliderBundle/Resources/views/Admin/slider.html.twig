{% extends "SWAdminBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Tous les diaporamas{% endblock %}

{% block spread_admin_content %}

	<section class="content-header">
		<h1 class="title">Liste de tous les diaporamas</h1>
      	<ol class="breadcrumb">
        	<li><a href="{{path('sw_admin_dashboard')}}"><i class="fa fa-dashboard"></i> Tableau de bord</a></li>
        	<li class="active">Tous les diaporamas</li>
      	</ol>
    </section>
	<section class="content">
	
		<div class="box">
                <div class="box-header">
                  <h3 class="box-title"></h3>
                </div><!-- /.box-header -->
                <div class="box-body">
                  <div id="example1_wrapper" class="dataTables_wrapper form-inline" role="grid">
                  <div class="col-md-12">
                  </div>
                  <div class="clearfix"></div>
                  <br>
                  <table id="example1" class="table table-bordered table-striped dataTable" aria-describedby="example1_info">
                    <thead>
                      <tr role="row">
	                      <th class="sorting_asc" role="columnheader" tabindex="0" aria-controls="example1" rowspan="1" colspan="1" aria-sort="ascending" aria-label="Rendering engine: activate to sort column descending">
	                      Id
	                      </th>
                        <th class="sorting" role="columnheader" tabindex="0" aria-controls="example1" rowspan="1" colspan="1" aria-label="Browser: activate to sort column ascending">Nom</th>
	                      <th class="sorting" role="columnheader" tabindex="0" aria-controls="example1" rowspan="1" colspan="1" aria-label="Browser: activate to sort column ascending">Position</th>
	                      <th style="width:130px">
	                      	Action
	                      </th>
                      </tr>
                    </thead>
                  <tbody role="alert" aria-live="polite" aria-relevant="all">
                    {% for slider in sliders %}
                  	  <tr>
                        <td class=" sorting_1">{{ slider.id }}</td>
                        <td class=" ">{{ slider.name }}</td>
                        <td class=" ">{{ slider.position }}</td>
                        <td>
                        <div class="btn-group">
                          <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                            Action <span class="caret"></span>
                          </button>
                          <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('sw_admin_slider_edit', { 'id' : slider.id }) }}">Editer</a></li>
                          </ul>
                        </div>
                        </td>
                      </tr>
					{% endfor %}
                    </tbody>
                    </table>
                    </div>
                </div><!-- /.box-body -->
              </div>
		
	</section>
{% endblock spread_admin_content %}
{% block javascripts %}
	{% javascripts 'plugins/datatables/jquery.dataTables.js''plugins/datatables/dataTables.bootstrap.js''plugins/slimScroll/jquery.slimscroll.min.js' %}
            <script type="text/javascript" src="{{ asset_url }}"></script>
        {% endjavascripts %}
{% endblock %}